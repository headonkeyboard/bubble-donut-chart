/* arbitrary number to build bubble donut */
export const DEFAULT_VIEWPORT = 600;

export const DEFAULT_DIAMETER = DEFAULT_VIEWPORT * 0.8;
export const DEFAULT_RADIUS = DEFAULT_DIAMETER / 2;
export const DONUT_CENTER = DEFAULT_VIEWPORT / 2;
